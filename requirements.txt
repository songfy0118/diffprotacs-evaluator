# DiffPROTACs Evaluator — Python dependencies
# Install with: pip install -r requirements.txt

# Core (required)
rdkit>=2022.09          # for xyz2mol / DetermineBonds; aromatic perception
numpy>=1.20

# Optional — DockStream depends on these in its own environment, NOT here.
# This evaluator calls DockStream via subprocess CLI, so we don't import it.
